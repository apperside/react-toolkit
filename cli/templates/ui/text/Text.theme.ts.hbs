import { TextStyleProps, TextTheme, VariantProps } from "apperside-react-toolkit";

declare module "apperside-react-toolkit" {
	interface TextVariants {
		custom: VariantProps<TextStyleProps>;
		title: VariantProps<TextStyleProps>;
	}

}

export const textTheme: TextTheme = {
  variants: {
    regular: {
      fontFamily: "body",
      // backgroundColor: "yellow",
    },
    bold: {
      fontFamily: "body",
      fontWeight: "bold"
    },
    medium: {
      fontFamily: "body",
      fontWeight: "medium"
    },
    black: {
      fontFamily: "body",
      fontWeight: "black"
    }
  }
};
