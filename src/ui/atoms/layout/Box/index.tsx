import { Box, HBox, VBox } from './Box';


/**
 * A Box with flex and forced vertical orientation
 */
// const VBox: React.FC<PropsOf<typeof Box>> = (props) => <Box display="flex" flexDirection="column" {...props} />;

export { Box, HBox, VBox };
